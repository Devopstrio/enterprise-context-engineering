apiVersion: v1
kind: Service
metadata:
  name: enterprise-context-engineering
spec:
  selector:
    app: context-engineering
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
