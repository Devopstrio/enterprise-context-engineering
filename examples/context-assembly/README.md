# Context Assembly Example

Demonstrates assembling a context window from multiple sources (system prompt, conversation memory, RAG retrieval documents, and user input) with automatic token budget allocation.
